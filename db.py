"""SQLite access for notes, drafts and runs. All DB access goes through here."""

from __future__ import annotations

# TODO(Phase 2): tables per PLAN.md §9; add_note, get_new_notes, set_note_status,
# add_draft, get_draft, set_draft_status, start_run, finish_run.
