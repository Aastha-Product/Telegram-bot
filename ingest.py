"""Capture: channel_post from the capture channel -> notes table."""

from __future__ import annotations

# TODO(Phase 3): handler that accepts only channel_post from settings.telegram_chat_id;
# text stored as-is, voice notes stored pending_transcription (transcribed in Phase 4).
